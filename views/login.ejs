<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MotoEV</title>
    <link rel="stylesheet" href="/login.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">
</head>
<body>
    <img src="/src/background.jpg" alt="" id="background">
    <div class="main">
        <div class="left">
            <div class="card">
                <h1>Log In</h1>
                <% if(error.length > 0 ){ %>
                    <p class="error"><%= error %></p>
                <% } %>
                <form action="/login" method="post">
                    <p>Username </p>
                    <input type="text" placeholder="email" name="username"> 
                    <p>Password</p>
                    <input type="Password" placeholder="password" name="password">
                    <button>Login →</button>
                </form>
                <div class="shortcut">
                    <i class='bx bxl-google' style='color:black'  ></i>
                    <i class='bx bxl-facebook-circle' style='color:black' ></i>
                    <i class='bx bxl-instagram-alt' style='color:black' ></i>
                </div>
                <a href="/signup">Don't have a account yet ? Sign Up</a>
            </div>
        </div>
        <div class="right">
            <img src="/src/Electric car-amico.png" alt=""> 
        </div>
    </div>
</body>
</html>